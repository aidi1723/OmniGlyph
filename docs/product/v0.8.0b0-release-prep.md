# v0.8.0b0 Release Preparation

This document records the release-prep state for the OmniGlyph v0.8.0 beta package candidate.

## Scope

- Branch: `codex/v0.8.0b0-release-prep`
- Base: `origin/main` after PR #7 merge.
- Package version: `0.8.0b0`
- Release label: `v0.8.0-beta`
- Publish status: not uploaded to TestPyPI, PyPI, or MCP Registry.
- Latest published PyPI version remains `0.6.0b0`.

## Release Candidate Contents

This candidate includes the merged v0.8 source batch:

- Policy Pack intent guardrails across Python, CLI, API, and MCP.
- Deterministic `parameters_schema` validation for intent parameters.
- Output guardrail policy modes for unknown, unapproved, and secret terms.
- Guardrail `review_packet` evidence for host review workflows.
- `omniglyph enforce-output` CLI evidence command.
- v0.7 release-hardening prerequisites: canonical release gate, artifact audit, wheel smoke, DLP redaction hardening, root restrictions, and packaging checks.

## Versioned Files

- `pyproject.toml`
- `src/omniglyph/__init__.py`
- `docs/api.md`
- `README.md`
- `README.zh-CN.md`
- `docs/ecosystem/pypi-publish.md`
- `docs/ecosystem/mcp-registry-submission.md`
- `package-registry/server.json`
- `docs/release-notes-v0.8-source-batch-draft.md`
- `CHANGELOG.md`

## Expected Artifacts

The release gate should build:

- `dist/omniglyph-0.8.0b0.tar.gz`
- `dist/omniglyph-0.8.0b0-py3-none-any.whl`

Upload exact filenames only. Do not use `dist/*`.

## Verification

Required before publication:

```bash
bash scripts/privacy-scan.sh
scripts/release_check.sh
```

Observed result:

- Privacy scan: pass.
- Release gate: pass.
- Python tests: `184 passed`.
- Ruff: pass.
- mypy: pass.
- `git diff --check`: pass.
- MCP smoke: 17 tools available.
- Build: `dist/omniglyph-0.8.0b0.tar.gz` and `dist/omniglyph-0.8.0b0-py3-none-any.whl`.
- Twine metadata check: pass.
- Artifact content audit: pass.
- Clean wheel install with CLI/MCP smoke: pass.
- Cross-border demo check: pass.

Note: old local `dist/omniglyph-0.7.0b0*` artifacts may still exist in a developer workspace. Upload exact `0.8.0b0` filenames only.

## Publication Boundary

This branch prepares release metadata and artifacts only. It does not publish packages.

Publication still requires explicit operator approval for:

1. TestPyPI upload.
2. Clean TestPyPI install verification.
3. PyPI upload.
4. Clean PyPI install verification.
5. MCP Registry submission or metadata PR.

## Rollback Notes

If release-prep is rejected before publication:

- Revert or close the release-prep PR.
- No registry rollback is required because no package has been uploaded.

If TestPyPI succeeds but PyPI publication is rejected:

- Keep the TestPyPI result as a verification record.
- Do not reuse the same public package version for a changed artifact.

If PyPI publication succeeds:

- Treat `0.8.0b0` as immutable.
- Fix any follow-up issue in a later version such as `0.8.1b0` or `0.8.0b1`.
